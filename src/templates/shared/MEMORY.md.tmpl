# Operator Memory

Durable facts about `{{operator_name}}` and `{{organization}}` worth keeping across sessions.

Updated as the agent learns. Pointers to specific notes, not the notes themselves.

---

## Operator profile

- Role: `{{operator_role}}`
- Organization: `{{organization}}`
- Timezone: `{{timezone}}`
- Tone preference: `{{tone}}`
- Pronouns: `{{operator_pronouns}}`

## Hierarchy

`{{hierarchy}}`

## Internal vocabulary / acronyms

`{{internal_acronyms}}`

## Inbox-triage defaults

- Urgent: `{{urgent_rules}}`
- Defer: `{{defer_rules}}`
- Delegate: `{{delegate_rules}}` → `{{delegate_recipient}}` (timeout: `{{delegate_timeout}}`)

## Sensitive data (never share externally)

`{{sensitive_data}}`

---

*Built up by the agent during operation. Atomic memory entries link here from `shared/memory/YYYY-MM-DD.md` daily notes.*
